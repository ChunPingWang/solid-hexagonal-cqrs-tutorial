// ============================================================
// Output Port - ID 產生器介面
// ============================================================
// 將 ID 的生成抽象化，方便測試時替換為可預測的實作。
// 遵循 DIP：核心不依賴具體的 ID 生成策略。
// ============================================================

export interface IdGenerator {
  generate(): string;
}
